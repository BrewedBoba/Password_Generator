**Pasword Generator**

A small project to help me understand how to use Tkinter library.

Once initialized, the password generator gui will come up where user can input how many characters they want in their password. The generator will then create a random password of the specified length. They can also specify if they want to include upper case characters, digits and symbols.

Next step: `implement a validation that checks if the password meets the specified criteria.`
